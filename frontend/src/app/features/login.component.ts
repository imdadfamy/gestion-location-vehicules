import { Component } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { Router } from '@angular/router'; import { AuthService } from '../core/auth.service';
@Component({standalone:true,imports:[FormsModule],template:`<main class="login"><section class="login-card"><img src="/fima-auto-logo.jpg" alt="FIMA AUTO"><div><span>ESPACE PROFESSIONNEL</span><h1>Bienvenue chez FIMA AUTO</h1><p>Gestion de location de véhicules.</p></div><form (ngSubmit)="go()"><label>E-mail</label><input class="form-control mb-3" name="email" [(ngModel)]="email" type="email" required><label>Mot de passe</label><input class="form-control mb-3" name="password" [(ngModel)]="password" type="password" required>@if(error){<p class="text-danger">{{error}}</p>}<button class="btn btn-primary w-100">Se connecter</button></form></section></main>`,styles:[`.login{min-height:100vh;display:grid;place-items:center;padding:24px;background:linear-gradient(135deg,#062b35,#0792a4)}.login-card{width:min(100%,470px);background:#fff;border-radius:24px;padding:38px;box-shadow:0 22px 60px rgba(0,0,0,.24)}.login-card img{height:66px;width:auto;margin-bottom:24px}.login-card span{font-size:.72rem;letter-spacing:.13em;color:#0792a4;font-weight:800}.login-card h1{font-size:1.7rem;margin:.45rem 0}.login-card p{color:#6b7c81;margin-bottom:26px}.login-card label{font-weight:700;font-size:.88rem;margin-bottom:6px}`]})export class LoginComponent{email='';password='';error='';constructor(private a:AuthService,private r:Router){}async go(){this.error=(await this.a.login(this.email,this.password))?.message||'';if(!this.error)this.r.navigateByUrl('/dashboard')}}
